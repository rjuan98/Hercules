<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Só aparece enquanto a captura automática não estiver ativada -->
    <Button
        android:id="@+id/btnAtivarNotificacoes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:text="🔔 Ativar captura automática"
        android:backgroundTint="@color/herc_barro"
        android:textColor="@color/herc_creme"
        android:visibility="gone" />

</FrameLayout>
